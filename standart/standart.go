package standart

func Min(first int, second int) int {
	if first < second {
		return first
	} else {
		return second
	}
}

func Max(first int, second int) int {
	if first > second {
		return first
	} else {
		return second
	}
}
